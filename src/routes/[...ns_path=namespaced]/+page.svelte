<!--
	Phase 3 stub: the +page.server.ts always either redirects (302 → legacy
	route) or throws a 404, so this template is unreachable. Phase 4+ cutovers
	replace the redirect with direct-serving loaders, at which point this
	template will render the entity page (per-namespace dispatch).
-->
<p>Loading…</p>
